# Feature Pool

`build_feature_pool.m` constructs the PVDF wavelet/statistical descriptors and
SG time-domain descriptors. `attach_feature_labels.m` aligns the resulting rows
with class, shape, material, and trial metadata.
