# Data Utilities

Functions in this module validate prepared artifacts, map global feature rows
back to raw trials, normalize channels from permitted source rows, and convert
episodes into the tensor, sequence, or graph-node formats required by each
baseline.
